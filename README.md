# Watcher

Stop context-switching. Your Slack conversations, triaged and waiting.

## Setup

1. Create a Slack App with a **User Token** (`xoxp-`) and add it to your config file at `~/.config/watcher/config.toml`

```toml
slack_user_token = "xoxp-your-token-here"
```
